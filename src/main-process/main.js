const yargs = require('yargs');
const path = require('path');
const start = require('./start');

// maybe this will useful in the future
// const args = yargs(process.argv)
//   .alias('d', 'dev')
//   .alias('t', 'test')
//   .argv

start();
